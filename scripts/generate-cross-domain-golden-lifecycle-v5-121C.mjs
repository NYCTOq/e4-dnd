import fs from "node:fs";import path from "node:path";
const root=process.cwd();const jsonPath=path.join(root,"certification-reports/cross-domain-golden-player-lifecycle-v5.121C.json");const report=JSON.parse(fs.readFileSync(jsonPath,"utf8"));if(report.releaseBlockers.length)throw new Error("v5.121C release blockers detected");if(report.profiles!==4||report.lifecycleCheckpoints!==28)throw new Error("v5.121C golden coverage mismatch");console.log(`v5.121C report GREEN: ${report.profiles} profiles, ${report.lifecycleCheckpoints} checkpoints, ${report.releaseBlockers.length} blockers.`);
