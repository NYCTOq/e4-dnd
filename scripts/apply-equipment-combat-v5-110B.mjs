import { readFile, writeFile } from "node:fs/promises";
import { resolve } from "node:path";
const path=resolve(process.cwd(),"package.json");const pkg=JSON.parse(await readFile(path,"utf8"));pkg.version="5.110.1";pkg.scripts??={};pkg.scripts["certify:equipment-combat:differential"]="vitest run src/certification/differential/equipmentCombatDifferential.test.ts";pkg.scripts["certify:equipment-combat:differential:report"]="node scripts/generate-equipment-combat-differential-report.mjs";pkg.scripts["certify:equipment-combat:full"]="npm run certify:equipment-combat:oracle && npm run certify:equipment-combat:matrix && npm run certify:equipment-combat:differential && npm run build && npm run certify:equipment-combat:report && npm run certify:equipment-combat:differential:report";await writeFile(path,JSON.stringify(pkg,null,2)+"\n","utf8");console.log("v5.110B equipment & combat differential matrix installed.");
