# v5.121D4 Cross-Domain UI E2E Class Selector Hotfix

Fixes the final two builder E2E failures by locating the class select inside the active Background & Class panel instead of assuming its accessible name is exactly `Class`.

No runtime behavior is changed.
