# Deployment Target

The executable package will be generated at:

`deployment/e4-dnd-6.2.0-rc1`

Upload the contents of that folder to the hosting document root or selected application subdirectory.

Do not upload:

- `node_modules`
- `test-results`
- Playwright traces
- source maps unless intentionally required
- local backups
- development environment files
