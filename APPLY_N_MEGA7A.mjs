import fs from "node:fs";
import path from "node:path";
const ROOT=process.cwd();
const adapterPath=path.join(ROOT,"src/core/rulesets/spellCharacterCombatAdapter.ts");
const panelPath=path.join(ROOT,"src/components/spells/SpellCastingRuntimePanel.tsx");
const testPath=path.join(ROOT,"src/core/rulesets/spellCastTransaction-N-MEGA7A.test.ts");
function req(f){if(!fs.existsSync(f))throw new Error("Required file not found: "+f);}
function rep(s,a,b,l){if(!s.includes(a))throw new Error("Patch anchor not found: "+l);return s.replace(a,b);}
const adapter_add="export type SpellSlotSource = \"spell\" | \"pact\";\n\nexport type SpellCastTransaction<T extends SpellCompatibleCharacter> = {\n  ok: boolean;\n  character: T;\n  reason: string | null;\n  slotSpent: boolean;\n  slotSource: SpellSlotSource | null;\n  castLevel: number;\n  replacedConcentrationSpellId: string | null;\n};\n\n/**\n * Applies a spell cast as one atomic character mutation. Invalid casts never\n * consume a slot or replace concentration. Cantrips use cast level 0 and do\n * not consume a slot. Leveled spells may use either normal or Pact slots.\n */\nexport function castCharacterSpell<T extends SpellCompatibleCharacter>(\n  character: T,\n  spell: CharacterSpellEntry,\n  requestedCastLevel = spell.level,\n  slotSource: SpellSlotSource = \"spell\",\n): SpellCastTransaction<T> {\n  const spellLevel = Math.max(0, int(spell.level));\n  const castLevel = spellLevel === 0 ? 0 : Math.max(0, int(requestedCastLevel));\n  const unchanged = structuredClone(character) as T;\n\n  if (!spell.id || !String(spell.id).trim()) {\n    return { ok: false, character: unchanged, reason: \"Büyü kimliği eksik.\", slotSpent: false, slotSource: null, castLevel, replacedConcentrationSpellId: null };\n  }\n  if (castLevel < spellLevel) {\n    return { ok: false, character: unchanged, reason: \"Büyü en az \" + spellLevel + \". seviye slot ister.\", slotSpent: false, slotSource, castLevel, replacedConcentrationSpellId: null };\n  }\n  if (spellLevel > 0 && !canCharacterCastSpell(character, spellLevel, castLevel, slotSource === \"pact\")) {\n    return { ok: false, character: unchanged, reason: castLevel + \". seviye \" + (slotSource === \"pact\" ? \"Pact\" : \"büyü\") + \" slotu kullanılamıyor.\", slotSpent: false, slotSource, castLevel, replacedConcentrationSpellId: null };\n  }\n\n  let next = spellLevel === 0\n    ? unchanged\n    : spendCharacterSpellSlot(character, castLevel, slotSource === \"pact\");\n  const replacedConcentrationSpellId = spell.concentration && character.concentrating\n    ? String(character.concentrationSpellId ?? \"\") || null\n    : null;\n  if (spell.concentration) next = setCharacterConcentration(next, String(spell.id)) as T;\n\n  return {\n    ok: true,\n    character: next as T,\n    reason: null,\n    slotSpent: spellLevel > 0,\n    slotSource: spellLevel > 0 ? slotSource : null,\n    castLevel,\n    replacedConcentrationSpellId,\n  };\n}\n\n";
const panel_import_old="  applyConcentrationDamage,\n  buildSpellRuntimeSnapshot,\n";
const panel_import_new="  applyConcentrationDamage,\n  buildSpellRuntimeSnapshot,\n  castCharacterSpell,\n";
const panel_state_old="  const [feedback, setFeedback] = useState(\"Büyü kaynakları hazır.\");\n";
const panel_state_new="  const [feedback, setFeedback] = useState(\"Büyü kaynakları hazır.\");\n  const availableSpells = Array.isArray(character.spells) ? character.spells : [];\n  const [selectedSpellId, setSelectedSpellId] = useState(String(availableSpells[0]?.id ?? \"\"));\n  const selectedSpell = availableSpells.find((spell) => String(spell.id) === selectedSpellId) ?? null;\n  const [castLevel, setCastLevel] = useState(Number(selectedSpell?.level ?? 0));\n  const [slotSource, setSlotSource] = useState<\"spell\" | \"pact\">(\"spell\");\n\n  const castableLevels = useMemo(() => {\n    if (!selectedSpell) return [];\n    if (selectedSpell.level === 0) return [0];\n    const slots = slotSource === \"pact\" ? snapshot.pactSlots : snapshot.spellSlots;\n    return slots\n      .filter((slot) => slot.level >= selectedSpell.level && slot.used < slot.max)\n      .map((slot) => slot.level);\n  }, [selectedSpell, slotSource, snapshot.pactSlots, snapshot.spellSlots]);\n\n  const selectSpell = (spellId: string) => {\n    setSelectedSpellId(spellId);\n    const spell = availableSpells.find((entry) => String(entry.id) === spellId);\n    setCastLevel(Number(spell?.level ?? 0));\n  };\n\n  const castSelectedSpell = () => {\n    if (!selectedSpell) { setFeedback(\"Kullanılacak büyüyü seç.\"); return; }\n    const transaction = castCharacterSpell(character, selectedSpell, castLevel, slotSource);\n    if (!transaction.ok) { setFeedback(transaction.reason ?? \"Büyü kullanılamadı.\"); return; }\n    onCharacterChange(transaction.character as T);\n    const upcast = transaction.castLevel > selectedSpell.level ? \" (\" + transaction.castLevel + \". seviyede)\" : \"\";\n    const replaced = transaction.replacedConcentrationSpellId ? \" Önceki konsantrasyon sona erdi: \" + transaction.replacedConcentrationSpellId + \".\" : \"\";\n    setFeedback(String(selectedSpell.name ?? selectedSpell.id) + upcast + \" kullanıldı.\" + replaced);\n  };\n";
const panel_ui_old="      <div className=\"spell-casting-runtime-panel__rest-actions\" aria-label=\"Büyü slotu dinlenme kontrolleri\">\n";
const panel_ui_new="      <section className=\"spell-casting-runtime-panel__cast\" aria-label=\"Büyü kullanma kontrolleri\" data-testid=\"spell-runtime-cast-controls\">\n        <h3>Büyü Kullan</h3>\n        {availableSpells.length === 0 ? <p>Karakterde kayıtlı büyü bulunmuyor.</p> : (\n          <div>\n            <label>Büyü\n              <select value={selectedSpellId} onChange={(event) => selectSpell(event.target.value)} data-testid=\"spell-runtime-spell-select\">\n                {availableSpells.map((spell) => <option key={String(spell.id)} value={String(spell.id)}>{spell.name ?? spell.id} (Seviye {spell.level})</option>)}\n              </select>\n            </label>\n            {selectedSpell && selectedSpell.level > 0 && (\n              <>\n                <label>Slot kaynağı\n                  <select value={slotSource} onChange={(event) => { const source = event.target.value as \"spell\" | \"pact\"; setSlotSource(source); setCastLevel(Number(selectedSpell.level)); }} data-testid=\"spell-runtime-slot-source\">\n                    <option value=\"spell\">Normal slot</option>\n                    {snapshot.pactSlots.length > 0 && <option value=\"pact\">Pact slotu</option>}\n                  </select>\n                </label>\n                <label>Kullanım seviyesi\n                  <select value={castLevel} onChange={(event) => setCastLevel(Number(event.target.value))} data-testid=\"spell-runtime-cast-level\">\n                    {castableLevels.length === 0 && <option value={selectedSpell.level}>Uygun slot yok</option>}\n                    {castableLevels.map((level) => <option key={level} value={level}>{level}. seviye</option>)}\n                  </select>\n                </label>\n              </>\n            )}\n            <button type=\"button\" onClick={castSelectedSpell} disabled={!selectedSpell || (selectedSpell.level > 0 && castableLevels.length === 0)} data-testid=\"spell-runtime-cast-button\">Kullan</button>\n          </div>\n        )}\n      </section>\n\n      <div className=\"spell-casting-runtime-panel__rest-actions\" aria-label=\"Büyü slotu dinlenme kontrolleri\">\n";
const test="import { describe, expect, it } from \"vitest\";\nimport { castCharacterSpell, type SpellCompatibleCharacter } from \"./spellCharacterCombatAdapter\";\n\nconst caster = (): SpellCompatibleCharacter => ({\n  id: \"caster\", level: 7, classId: \"wizard\", spellSlots: [\n    { level: 1, max: 4, used: 4 },\n    { level: 2, max: 3, used: 1 },\n    { level: 3, max: 3, used: 0 },\n  ], pactSlots: [{ level: 3, max: 2, used: 0, pact: true }],\n  concentrating: true, concentrationSpellId: \"bless\",\n});\n\ndescribe(\"N-MEGA7A atomic spell cast transaction\", () => {\n  it(\"upcasts with an available normal slot and replaces concentration atomically\", () => {\n    const before = caster();\n    const result = castCharacterSpell(before, { id: \"hold-person\", name: \"Hold Person\", level: 2, concentration: true }, 3, \"spell\");\n    expect(result.ok).toBe(true);\n    expect(result.slotSpent).toBe(true);\n    expect(result.castLevel).toBe(3);\n    expect(result.replacedConcentrationSpellId).toBe(\"bless\");\n    expect(result.character.spellSlots?.find((slot) => slot.level === 3)?.used).toBe(1);\n    expect(result.character.concentrationSpellId).toBe(\"hold-person\");\n    expect(before.spellSlots?.find((slot) => slot.level === 3)?.used).toBe(0);\n  });\n\n  it(\"casts a cantrip without consuming any slot\", () => {\n    const before = caster();\n    const result = castCharacterSpell(before, { id: \"fire-bolt\", level: 0 }, 0, \"spell\");\n    expect(result.ok).toBe(true);\n    expect(result.slotSpent).toBe(false);\n    expect(result.character.spellSlots).toEqual(before.spellSlots);\n  });\n\n  it(\"uses Pact Magic as a separate source\", () => {\n    const before = caster();\n    const result = castCharacterSpell(before, { id: \"armor-of-agathys\", level: 1 }, 3, \"pact\");\n    expect(result.ok).toBe(true);\n    expect(result.character.pactSlots?.[0].used).toBe(1);\n    expect(result.character.spellSlots).toEqual(before.spellSlots);\n  });\n\n  it(\"rejects unavailable and under-level casts without mutating resources or concentration\", () => {\n    const before = caster();\n    const unavailable = castCharacterSpell(before, { id: \"shield\", level: 1 }, 1, \"spell\");\n    const underLevel = castCharacterSpell(before, { id: \"fireball\", level: 3 }, 2, \"spell\");\n    expect(unavailable.ok).toBe(false);\n    expect(underLevel.ok).toBe(false);\n    expect(unavailable.character.spellSlots).toEqual(before.spellSlots);\n    expect(unavailable.character.concentrationSpellId).toBe(\"bless\");\n  });\n});\n";
req(adapterPath); req(panelPath);
let adapter=fs.readFileSync(adapterPath,"utf8");
if(!adapter.includes("export function castCharacterSpell")){adapter=rep(adapter,"export function setCharacterConcentration<\n",adapter_add+"export function setCharacterConcentration<\n","adapter");fs.writeFileSync(adapterPath,adapter);}
let panel=fs.readFileSync(panelPath,"utf8");
if(!panel.includes("spell-runtime-cast-button")){panel=rep(panel,panel_import_old,panel_import_new,"import");panel=rep(panel,panel_state_old,panel_state_new,"state");panel=rep(panel,panel_ui_old,panel_ui_new,"ui");fs.writeFileSync(panelPath,panel);}
fs.writeFileSync(testPath,test);
console.log("N-MEGA7A atomic spell casting files applied.");
