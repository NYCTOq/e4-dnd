import{describe,expect,it}from"vitest";import{getUiCatalogCoverage,translateUiText}from"./uiTextCatalog";
describe("player journey UI catalog",()=>{it("translates exact labels without losing whitespace",()=>expect(translateUiText("en","  Karakter Oluştur ")).toBe("  Create Character "));it("translates dynamic counters",()=>expect(translateUiText("en","3 hata · 2 uyarı")).toBe("3 errors · 2 warnings"));it("never changes Turkish mode or unknown content",()=>{expect(translateUiText("tr","Karakterler")).toBe("Karakterler");expect(translateUiText("en","Nika's Custom Lore")).toBe("Nika's Custom Lore")});it("reports measurable catalog coverage",()=>expect(getUiCatalogCoverage(["Karakterler","Karakterler","Bilinmeyen"])) .toEqual({total:2,translated:1,missing:["Bilinmeyen"]}))});
