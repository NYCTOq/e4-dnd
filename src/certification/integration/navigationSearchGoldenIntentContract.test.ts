import { existsSync,readFileSync } from "node:fs";import { describe,expect,it } from "vitest";
const files=["src/shared/navigation/navigationSearchAliases.ts","src/certification/golden/navigationSearchGoldenIntentIntegration.ts","src/certification/golden/navigationSearchGoldenIntentIntegration.test.ts","scripts/generate-navigation-search-golden-v5-123C.mjs","certification-reports/navigation-search-golden-intent-v5.123C.json","certification-reports/navigation-search-golden-intent-v5.123C.md"];
describe("v5.123C golden search intent artifact contract",()=>{for(const file of files)it(`${file} exists`,()=>expect(existsSync(file)).toBe(true));it("release report has no blockers",()=>{const report=JSON.parse(readFileSync("certification-reports/navigation-search-golden-intent-v5.123C.json","utf8"));expect(report.blockers).toEqual([]);});});
