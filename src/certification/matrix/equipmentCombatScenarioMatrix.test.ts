import {describe,it,expect} from "vitest";import {EQUIPMENT_COMBAT_SCENARIOS} from "./equipmentCombatScenarioMatrix";describe("equipment combat matrix",()=>{it("broad coverage",()=>expect(EQUIPMENT_COMBAT_SCENARIOS.length).toBeGreaterThanOrEqual(250));it("unique ids",()=>{const ids=EQUIPMENT_COMBAT_SCENARIOS.map(x=>x.id);expect(new Set(ids).size).toBe(ids.length)});it.each(EQUIPMENT_COMBAT_SCENARIOS)("$id stable",s=>{expect(s.actual).not.toBeUndefined();expect(["number","string"]).toContain(typeof s.actual)})});
