import{describe,expect,it}from"vitest";import{getFeatRuntime}from"./featRuntimeRules";
describe("feat runtime",()=>{it("applies 2014 Alert, Mobile and Tough",()=>{expect(getFeatRuntime(["Alert","Mobile","Tough"],10,"dnd_2014")).toMatchObject({alertInitiativeBonus:5,speedBonus:10,toughHpBonus:20})});it("uses proficiency for 2024 Alert and Lucky",()=>{expect(getFeatRuntime(["Alert","Lucky"],9,"dnd_2024")).toMatchObject({alertInitiativeBonus:4,luckyUses:4})});it("keeps unrelated feats neutral",()=>{expect(getFeatRuntime(["Actor"],5,"dnd_2014")).toEqual({alertInitiativeBonus:0,speedBonus:0,toughHpBonus:0,luckyUses:0,passivePerceptionBonus:0})})});
