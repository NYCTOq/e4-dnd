import{describe,expect,it}from"vitest";import type{CharacterSpellEffect}from"../character/character.types";import{getItemEffectRuntime,getItemTempHp,removeFragileItemEffects}from"./itemEffectRuntimeRules";
const effect=(spellId:string)=>({id:spellId,spellId,name:spellId,remainingRounds:10,concentration:false,summary:""})as CharacterSpellEffect;
describe("item effect runtime",()=>{it("applies speed potion combat bonuses",()=>{expect(getItemEffectRuntime([effect("item:potion-speed")])).toMatchObject({armorClassBonus:2,speedMultiplier:2,dexSaveMode:"advantage"})});it("combines advanced potion effects",()=>{expect(getItemEffectRuntime([effect("item:potion-invisibility"),effect("item:potion-heroism"),effect("item:potion-diminution"),effect("item:potion-gaseous-form")])).toMatchObject({attackMode:"advantage",attackSaveBonusDice:"1d4",weaponDamagePenaltyDice:"1d4",blocksAggressiveActions:true,damageResistance:true})});it("removes potion invisibility after an aggressive action",()=>{expect(removeFragileItemEffects([effect("item:potion-invisibility"),effect("spell:bless")]).map(item=>item.spellId)).toEqual(["spell:bless"])});it("grants heroism temporary hit points",()=>{expect(getItemTempHp({id:"potion-heroism"}as never)).toBe(10);expect(getItemTempHp({id:"other"}as never)).toBe(0)})});
