import type{AbilityKey}from"../character/character.types";import type{DndRaceData}from"./ruleset.types";import type{RollMode}from"./attackResolution";
export type AncestryRuntime={speedBonus:number;darkvision:number;damageResistances:string[];saveAdvantages:AbilityKey[];criticalExtraDice:number;maxHpBonus:number;features:string[]};
const has=(traits:string[],pattern:RegExp)=>traits.some(trait=>pattern.test(trait));
export function getAncestryRuntime(race:DndRaceData|undefined,subraceName:string|undefined,level:number):AncestryRuntime{if(!race)return{speedBonus:0,darkvision:0,damageResistances:[],saveAdvantages:[],criticalExtraDice:0,maxHpBonus:0,features:[]};const subrace=race.subraces?.find(item=>item.name===subraceName);const traits=[...race.traits,...(subrace?.traits??[])];const resistances:string[]=[];if(has(traits,/hellish resistance/i))resistances.push("fire");if(has(traits,/celestial resistance/i))resistances.push("radiant","necrotic");if(has(traits,/dwarven resilience|stout resilience/i))resistances.push("poison");const saves:AbilityKey[]=has(traits,/gnome cunning|gnomish cunning/i)?["int","wis","cha"]:[];if(has(traits,/dwarven resilience|stout resilience/i))saves.push("con");return{speedBonus:has(traits,/fleet of foot/i)?5:0,darkvision:has(traits,/superior darkvision/i)?Math.max(120,race.darkvision??0):race.darkvision??0,damageResistances:[...new Set(resistances)],saveAdvantages:[...new Set(saves)],criticalExtraDice:has(traits,/savage attacks/i)?1:0,maxHpBonus:has(traits,/dwarven toughness/i)?Math.max(1,Math.min(20,Math.floor(level))):0,features:traits.filter(trait=>/relentless endurance|luck|brave|fey ancestry|breath weapon|healing hands|adrenaline rush|large form|draconic flight/i.test(trait))}}
export function getAncestrySaveMode(runtime:AncestryRuntime,ability:AbilityKey,current:RollMode):RollMode{if(!runtime.saveAdvantages.includes(ability))return current;return current==="disadvantage"?"normal":"advantage"}
export function reduceAncestryDamage(amount:number,damageType:string,runtime:AncestryRuntime){const safe=Math.max(0,Math.floor(amount));return runtime.damageResistances.includes(damageType.toLowerCase())?Math.ceil(safe/2):safe}
