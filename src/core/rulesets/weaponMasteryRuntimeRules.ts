import type{DndItemData,WeaponMastery}from"./ruleset.types";import{getWeaponMastery}from"./equipmentRules";
export type WeaponMasteryOutcome={mastery:WeaponMastery|null;grazeDamage:number;grantsVex:boolean;note:string|null};
export function resolveWeaponMastery(input:{weapon:DndItemData;ruleset:string;masteredWeaponIds:string[];hit:boolean;abilityModifier:number}):WeaponMasteryOutcome{const mastery=input.masteredWeaponIds.includes(input.weapon.id)?getWeaponMastery(input.weapon,input.ruleset):null;if(!mastery)return{mastery:null,grazeDamage:0,grantsVex:false,note:null};if(mastery==="Graze"&&!input.hit)return{mastery,grazeDamage:Math.max(0,input.abilityModifier),grantsVex:false,note:"Graze: miss olsa da ability modifier kadar damage."};if(mastery==="Vex"&&input.hit)return{mastery,grazeDamage:0,grantsVex:true,note:"Vex: aynı hedefe sonraki attack advantage."};const notes:Record<WeaponMastery,string>={Cleave:"Cleave: yakındaki ikinci hedefe ek attack açılır.",Graze:"Graze yalnızca miss durumunda tetiklenir.",Nick:"Nick: Light ek attack Bonus Action harcamaz.",Push:"Push: hedef Large veya küçükse 10 ft. itilir.",Sap:"Sap: hedefin sonraki attack rollu disadvantage.",Slow:"Slow: hedefin speed değeri sonraki tura kadar 10 ft. azalır.",Topple:"Topple: hedef CON save ile Prone olmamaya çalışır.",Vex:"Vex yalnızca hit durumunda tetiklenir."};return{mastery,grazeDamage:0,grantsVex:false,note:input.hit?notes[mastery]:null}}
