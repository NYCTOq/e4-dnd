import type{RulesetId}from"../character/character.types";
export const PACT_MAGIC_TABLE:Record<number,{slotLevel:number;slots:number}>={1:{slotLevel:1,slots:1},2:{slotLevel:1,slots:2},3:{slotLevel:2,slots:2},4:{slotLevel:2,slots:2},5:{slotLevel:3,slots:2},6:{slotLevel:3,slots:2},7:{slotLevel:4,slots:2},8:{slotLevel:4,slots:2},9:{slotLevel:5,slots:2},10:{slotLevel:5,slots:2},11:{slotLevel:5,slots:3},12:{slotLevel:5,slots:3},13:{slotLevel:5,slots:3},14:{slotLevel:5,slots:3},15:{slotLevel:5,slots:3},16:{slotLevel:5,slots:3},17:{slotLevel:5,slots:4},18:{slotLevel:5,slots:4},19:{slotLevel:5,slots:4},20:{slotLevel:5,slots:4}};
export function getPactMagicSlots(className:string,level:number){if(className.trim().toLowerCase()!=="warlock")return[];const row=PACT_MAGIC_TABLE[Math.max(1,Math.min(20,Math.floor(level)))];return[{level:row.slotLevel,max:row.slots,used:0}]}
export function getMysticArcanumLevels(className:string,level:number,_ruleset:RulesetId){if(className.trim().toLowerCase()!=="warlock")return[];return[6,7,8,9].filter((_,index)=>level>=11+index*2)}
