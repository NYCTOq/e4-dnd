import{describe,expect,it}from"vitest";import{classifyFeat,classifyItem,classifySpell,getRuntimeCoverageCertification}from"./runtimeCoverageCertification";import type{DndFeatData,DndItemData,DndSpellData,RulesetData}from"./ruleset.types";
const feat=(name:string,benefits=["Benefit"]):DndFeatData=>({id:name.toLowerCase(),name,ruleset:"dnd_2014",category:"general",summary:"",benefits});const spell=(patch:Partial<DndSpellData>):DndSpellData=>({id:"spell",name:"Spell",level:1,school:"Evocation",castingTime:"1 action",range:"60 ft",components:[],duration:"Instantaneous",concentration:false,ritual:false,classes:["Wizard"],description:"Description",...patch});const item=(patch:Partial<DndItemData>):DndItemData=>({id:"item",name:"Item",category:"gear",cost:"1 gp",weight:1,description:"Description",...patch});
describe("runtime coverage certification",()=>{it("separates automatic, assisted and manual feats",()=>{expect(classifyFeat(feat("Alert")).tier).toBe("automatic");expect(classifyFeat({...feat("Skilled"),choiceType:"skills"}).tier).toBe("assisted");expect(classifyFeat(feat("Narrative Gift")).tier).toBe("manual")});it("recognizes resolved and guided spell behavior",()=>{expect(classifySpell(spell({effectType:"damage",attackType:"saving-throw",damageDice:"3d6"})).tier).toBe("automatic");expect(classifySpell(spell({effectType:"movement"})).tier).toBe("assisted");expect(classifySpell(spell({})).tier).toBe("manual")});it("recognizes mechanical item metadata",()=>{expect(classifyItem(item({healingFormula:"2d4+2"})).tier).toBe("automatic");expect(classifyItem(item({category:"pack"})).tier).toBe("assisted")});it("never certifies an empty ruleset",()=>{const data={id:"dnd_2014",name:"Empty",classes:[],subclasses:[],races:[],backgrounds:[],feats:[],spells:[],items:[],monsters:[]}as RulesetData;expect(getRuntimeCoverageCertification(data)).toMatchObject({score:0,status:"needs-work"})})});
