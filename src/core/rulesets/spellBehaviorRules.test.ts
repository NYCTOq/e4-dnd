import{describe,expect,it}from"vitest";import{getSpellBehavior}from"./spellBehaviorRules";import type{DndSpellData}from"./ruleset.types";const spell=(patch:Partial<DndSpellData>):DndSpellData=>({id:"x",name:"Test",level:1,school:"Test",castingTime:"1 Action",range:"60 ft.",components:[],duration:"Instantaneous",concentration:false,ritual:false,classes:[],description:"",...patch});describe("spell behavior",()=>{it("scales additional targets with slot level",()=>expect(getSpellBehavior(spell({target:"1 creature",scaling:{mode:"slot",additionalTargetsPerStep:1}}),4).targetCount).toBe(4));it("identifies ritual, repeat save and material consumption",()=>expect(getSpellBehavior(spell({ritual:true,description:"The target repeats the saving throw at the end of its turn.",materialCost:"100 gp",materialConsumed:true}))).toMatchObject({ritual:true,repeatSave:true,materialWarning:"100 gp · consumed"}));it("classifies summoning and movement",()=>{expect(getSpellBehavior(spell({effectType:"summoning"})).summoning).toBe(true);expect(getSpellBehavior(spell({name:"Misty Step",effectType:"movement"})).movement).toBe(true)})});
