import type{DndFeatData}from"./ruleset.types";import{getProficiencyBonus}from"../character/characterCalculator";
export type FeatAction={id:string;name:string;type:"action"|"bonus-action"|"reaction";maxUses:number;summary:string};
export type AdvancedFeatRuntime={armorClassBonus:number;concentrationAdvantage:boolean;mentalSaveAdvantage:boolean;ignoresLoading:boolean;spellRangeMultiplier:number;powerAttack:boolean;actions:FeatAction[]};
export function getAdvancedFeatRuntime(feats:DndFeatData[],level:number):AdvancedFeatRuntime{const names=new Set(feats.map(feat=>feat.name.toLowerCase()));const has=(pattern:RegExp)=>[...names].some(name=>pattern.test(name));const pb=getProficiencyBonus(level);const actions:FeatAction[]=[];if(has(/defensive duelist/))actions.push({id:"defensive-duelist",name:"Defensive Duelist",type:"reaction",maxUses:99,summary:`Reaction ile AC +${pb}`});if(has(/inspiring leader/))actions.push({id:"inspiring-leader",name:"Inspiring Leader",type:"action",maxUses:1,summary:"Partiye temporary HP ver."});if(has(/shield master/))actions.push({id:"shield-master",name:"Shield Master Shove",type:"bonus-action",maxUses:99,summary:"Shield ile hedefi it."});if(has(/polearm master/))actions.push({id:"polearm-master",name:"Polearm Bonus Attack",type:"bonus-action",maxUses:99,summary:"Polearm sapıyla bonus saldırı."});if(has(/telekinetic/))actions.push({id:"telekinetic-shove",name:"Telekinetic Shove",type:"bonus-action",maxUses:99,summary:"30 ft. içindeki hedefi zihinsel olarak it."});if(has(/sentinel|mage slayer/))actions.push({id:"reactive-strike",name:has(/sentinel/)?"Sentinel Strike":"Mage Slayer Strike",type:"reaction",maxUses:99,summary:"Tetiklenen reaction saldırısı."});return{armorClassBonus:has(/dual wielder/)?1:0,concentrationAdvantage:has(/war caster|mage slayer/),mentalSaveAdvantage:has(/mage slayer/),ignoresLoading:has(/crossbow expert/),spellRangeMultiplier:has(/spell sniper/)?2:1,powerAttack:has(/great weapon master|sharpshooter/)&&feats.some(feat=>feat.ruleset==="dnd_2014"),actions}}
export function getInspiringLeaderTempHp(level:number,chaModifier:number){return Math.max(1,level+Math.max(0,chaModifier))}
