import{describe,expect,it}from"vitest";import type{DndItemData}from"./ruleset.types";import{consumeInventoryItem,createItemEffect,getItemDamageProfile,getItemHealingFormula,getItemRestoration,isConsumableItem}from"./itemUseRules";
describe("item use",()=>{it("recognizes consumables and ammunition",()=>{expect(isConsumableItem({category:"gear",tags:["Consumable"]}as DndItemData)).toBe(true);expect(isConsumableItem({category:"ammunition"}as DndItemData)).toBe(true)});it("extracts healing dice and prefers structured formulas",()=>{expect(getItemHealingFormula({description:"Restores 2d4 + 2 hit points."}as DndItemData)).toBe("2d4+2");expect(getItemHealingFormula({description:"No roll text.",healingFormula:"10d4 + 20"}as DndItemData)).toBe("10d4+20")});it("returns restorative condition and recovery metadata",()=>{expect(getItemRestoration({curesConditions:["Poisoned"],clearsExhaustion:true,restoresHitDice:true}as DndItemData)).toEqual({curesConditions:["Poisoned"],clearsExhaustion:true,restoresHitDice:true})});it("builds offensive consumable damage profiles",()=>{expect(getItemDamageProfile({itemDamageFormula:"3d6",extraDamageType:"thunder",itemSaveAbility:"con",itemSaveDc:13,itemSaveRule:"half"}as DndItemData)).toEqual({formula:"3d6",damageType:"thunder",saveAbility:"con",saveDc:13,saveRule:"half",targetTag:null})});it("decrements and removes inventory entries",()=>{expect(consumeInventoryItem([{itemId:"arrow",quantity:2}],"arrow")).toEqual([{itemId:"arrow",quantity:1}]);expect(consumeInventoryItem([{itemId:"arrow",quantity:1}],"arrow")).toEqual([])});it("creates a timed active effect from potion metadata",()=>{const effect=createItemEffect({id:"potion-speed",name:"Potion of Speed",category:"gear",description:"",effectDurationRounds:10,effectSummary:"Speed doubles."}as DndItemData);expect(effect).toMatchObject({spellId:"item:potion-speed",name:"Potion of Speed",remainingRounds:10,concentration:false,summary:"Speed doubles."})})});
