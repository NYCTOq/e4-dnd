import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";import{getRuntimeCoverageCertification}from"./runtimeCoverageCertification";import{enrichClassProgression}from"./classProgressionAudit";import{SUBCLASS_EXPANSION_2014,SUBCLASS_EXPANSION_2024}from"./subclassExpansion";import{FEAT_EXPANSION_2014,FEAT_EXPANSION_2024}from"./featExpansion";import{SPELL_EXPANSION_2014,SPELL_EXPANSION_2024}from"./spellExpansion";import{ITEM_EXPANSION_2014,ITEM_EXPANSION_2024}from"./itemExpansion";import type{RulesetData}from"./ruleset.types";
const read=<T>(edition:string,file:string)=>JSON.parse(readFileSync(new URL(`../../../public/data/${edition}/${file}.json`,import.meta.url),"utf8"))as T[];const merge=<T extends{id:string}>(base:T[],extra:T[])=>[...base,...extra.filter(item=>!base.some(existing=>existing.id===item.id))];function load(edition:"dnd_2014"|"dnd_2024"):RulesetData{return{id:edition,name:edition,classes:read<RulesetData["classes"][number]>(edition,"classes").map(item=>enrichClassProgression(item,edition)),subclasses:merge(read(edition,"subclasses"),edition==="dnd_2014"?SUBCLASS_EXPANSION_2014:SUBCLASS_EXPANSION_2024),races:read(edition,"races"),backgrounds:read(edition,"backgrounds"),feats:merge(read(edition,"feats"),edition==="dnd_2014"?FEAT_EXPANSION_2014:FEAT_EXPANSION_2024),spells:merge(read(edition,"spells"),edition==="dnd_2014"?SPELL_EXPANSION_2014:SPELL_EXPANSION_2024),items:merge(read(edition,"items"),edition==="dnd_2014"?ITEM_EXPANSION_2014:ITEM_EXPANSION_2024),monsters:read(edition,"monsters")}}
describe("real runtime coverage matrix",()=>{for(const edition of["dnd_2014","dnd_2024"]as const)it(`${edition} classifies every loaded runtime entity`,()=>{const report=getRuntimeCoverageCertification(load(edition));expect(report.categories).toHaveLength(5);expect(report.categories.find(item=>item.id==="classes")).toMatchObject({total:12,automatic:12,missing:0});expect(report.categories.every(group=>group.total===group.automatic+group.assisted+group.manual+group.missing)).toBe(true);expect(report.score).toBeGreaterThan(45)})});
