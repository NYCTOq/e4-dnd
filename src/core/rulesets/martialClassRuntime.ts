import{getBarbarianCombatFeatures,getBrutalCriticalExtraDice,getRageDamageBonus}from"./barbarianRules";import{getActionSurgeUses,getFighterCombatFeatures}from"./fighterRules";import{getMartialArtsDie,getMonkCombatFeatures}from"./monkRules";import{getRogueCombatFeatures,getSneakAttackDice}from"./rogueRules";
export type MartialClassRuntime={className:string;attacksPerAction:number;primaryResourceMax:number;damageBonus:string|null;reactionDefense:boolean;notes:string[]};
export function getMartialClassRuntime(className:string,level:number,ruleset:string):MartialClassRuntime{const key=className.trim().toLowerCase(),safe=Math.max(1,Math.min(20,Math.floor(level)));let attacksPerAction=1,primaryResourceMax=0,damageBonus:string|null=null,reactionDefense=false;const notes:string[]=[];if(key==="fighter"){const x=getFighterCombatFeatures(safe);attacksPerAction=x.extraAttack;primaryResourceMax=getActionSurgeUses(safe);if(x.indomitable)notes.push(`Indomitable ${x.indomitable}`)}if(key==="barbarian"){const x=getBarbarianCombatFeatures(safe);attacksPerAction=x.extraAttack?2:1;primaryResourceMax=safe>=17?6:safe>=12?5:safe>=6?4:safe>=3?3:2;damageBonus=`Rage +${getRageDamageBonus(safe)} · Brutal +${getBrutalCriticalExtraDice(safe,ruleset)} die`;reactionDefense=x.relentlessRage}if(key==="rogue"){const x=getRogueCombatFeatures(safe);damageBonus=`Sneak Attack ${getSneakAttackDice(safe)}d6`;reactionDefense=x.uncannyDodge;if(x.reliableTalent)notes.push("Reliable Talent")}if(key==="monk"){const x=getMonkCombatFeatures(safe);attacksPerAction=safe>=5?2:1;primaryResourceMax=safe>=2?safe:0;damageBonus=`Martial Arts d${getMartialArtsDie(safe,ruleset)}`;reactionDefense=x.deflectAttacks;if(x.stunningStrike)notes.push("Stunning Strike")}return{className,attacksPerAction,primaryResourceMax,damageBonus,reactionDefense,notes}}
