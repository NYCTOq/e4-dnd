import{describe,expect,it}from"vitest";import type{DndItemData}from"./ruleset.types";import{getMagicWeaponExtraDamage}from"./magicWeaponRules";
const weapon=(patch:Partial<DndItemData>)=>({id:"magic",name:"Magic Weapon",category:"weapon",cost:"rare",weight:3,description:"",...patch})as DndItemData;
describe("magic weapon rules",()=>{it("returns always-on elemental damage",()=>{expect(getMagicWeaponExtraDamage(weapon({extraDamageDice:"2d6",extraDamageType:"fire"}),"normal")).toEqual([{formula:"2d6",damageType:"fire",source:"always"}])});it("requires the matching slayer target",()=>{const slayer=weapon({extraDamageDice:"3d6",extraDamageType:"slashing",extraDamageTargetTag:"dragon"});expect(getMagicWeaponExtraDamage(slayer,"normal")).toEqual([]);expect(getMagicWeaponExtraDamage(slayer,"dragon")).toHaveLength(1)});it("adds critical-only damage on a critical hit",()=>{const vicious=weapon({criticalExtraDamageDice:"2d6",extraDamageType:"slashing"});expect(getMagicWeaponExtraDamage(vicious,"normal",false)).toEqual([]);expect(getMagicWeaponExtraDamage(vicious,"normal",true)[0]).toMatchObject({formula:"2d6",source:"critical"})})});
