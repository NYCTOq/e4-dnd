import{describe,expect,it}from"vitest";import{getFavoredEnemyUses,getHuntersMarkDamage,getRangerCombatFeatures}from"./rangerRules";
describe("ranger rules",()=>{it("sets Favored Enemy uses from Wisdom",()=>{expect(getFavoredEnemyUses(-1)).toBe(1);expect(getFavoredEnemyUses(4)).toBe(4)});it("scales Hunter's Mark damage",()=>{expect(getHuntersMarkDamage(1)).toBe("1d6");expect(getHuntersMarkDamage(13)).toBe("1d8");expect(getHuntersMarkDamage(20)).toBe("1d10")});it("keeps Favored Enemy edition-aware",()=>{expect(getRangerCombatFeatures(5,"dnd_2024")).toMatchObject({favoredEnemy:true,extraAttack:true});expect(getRangerCombatFeatures(5,"dnd_2014").favoredEnemy).toBe(false)})});
