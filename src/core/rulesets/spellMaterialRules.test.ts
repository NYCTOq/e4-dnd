import{describe,expect,it}from"vitest";import{getSpellMaterialReadiness}from"./spellMaterialRules";import type{DndItemData,DndSpellData}from"./ruleset.types";const spell=(patch:Partial<DndSpellData>):DndSpellData=>({id:"spell",name:"Spell",level:1,school:"Abjuration",castingTime:"1 action",range:"Self",components:["V","S"],duration:"Instantaneous",concentration:false,ritual:false,classes:["Wizard"],description:"",...patch});const item=(id:string,name:string):DndItemData=>({id,name,category:"gear",cost:"1 gp",weight:1,description:""});describe("spell material readiness",()=>{it("ignores spells without material components",()=>expect(getSpellMaterialReadiness(spell({}),[],[]).status).toBe("not-required"));it("accepts a focus for ordinary material components",()=>expect(getSpellMaterialReadiness(spell({components:["M"],material:"a feather"}),[{itemId:"focus",quantity:1}],[item("focus","Arcane Focus")]).status).toBe("focus-ready"));it("flags costly consumed materials for review",()=>expect(getSpellMaterialReadiness(spell({components:["M"],material:"diamond dust",materialCost:"100 gp",materialConsumed:true}),[],[])).toMatchObject({status:"needs-review",costly:true,consumed:true}));it("matches a specifically owned component",()=>expect(getSpellMaterialReadiness(spell({components:["M"],material:"diamond"}),[{itemId:"diamond",quantity:1}],[item("diamond","Diamond")]).status).toBe("item-ready"))});
