# E4 D&D v5.123D1

This hotfix replaces the invalid `npm.cmd run npx -- playwright install chromium` invocation with the actual executable call `npx.cmd playwright install chromium`.

Extract over the project root and run:

```powershell
powershell -ExecutionPolicy Bypass -File .\APPLY_NAVIGATION_SEARCH_UI_E2E_FINAL_CLOSURE_HOTFIX_v5.123D1.ps1
```
