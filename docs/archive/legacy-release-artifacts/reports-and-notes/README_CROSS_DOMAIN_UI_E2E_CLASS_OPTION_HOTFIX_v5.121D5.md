# E4 D&D v5.121D5 Cross-Domain UI E2E Class Option Hotfix

This hotfix replaces the final brittle Builder class selector with a DOM contract based on the unique `Class seç` option rendered by the real class select.

## Scope

- Keeps the six already-green cross-domain UI scenarios unchanged.
- Resolves the Builder class select identically on desktop and mobile.
- Runs the complete v5.121 final certification chain.
