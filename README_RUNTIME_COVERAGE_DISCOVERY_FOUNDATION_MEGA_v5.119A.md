# E4 D&D v5.119A - Runtime Coverage Discovery Foundation

This package starts the feat, spell, item and subclass runtime-coverage series.
It inventories both editions, classifies every loaded entity as automatic,
assisted, manual or missing, preserves missing behavior as a release blocker,
and locks v5.119B as Runtime Differential and Missing Behavior Closure.

Apply from the project root:

```powershell
powershell -ExecutionPolicy Bypass -File .\APPLY_RUNTIME_COVERAGE_DISCOVERY_FOUNDATION_MEGA_v5.119A.ps1
```
